pipeline {
    agent {
        label 'growlt189'
    }
 
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
 
        stage('Build APK') {
            steps {
                script {
                   
                    sh "/opt/flutter/flutter/bin/flutter clean"
                    sh "/opt/flutter/flutter/bin/flutter pub get"
                    sh "/opt/flutter/flutter/bin/flutter build apk"
                }
            }
        }
 
        stage('Deploy') {
            steps {
                script {
                    dir('/home/jenkins/jenkins/workspace/growlt189') {
                        sh 'fastlane deploy_to_play_store'
                    }
                }
            }
        }
    }
 
    post {
        success {
            echo 'Build and deployment successful!'
        }
        failure {
            echo 'Build or deployment failed!'
        }
    }
}
