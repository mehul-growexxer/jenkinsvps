pipeline {
    agent {
        label 'growlt189'
    }

    stages {
        stage('Cleanup Workspace') {
            steps {
                cleanWs()
            }
        }

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Debug') {
            steps {
                echo 'Current workspace:'
                sh 'ls -al'

                echo 'Contents of android directory:'
                sh 'ls -al android'

                echo 'Contents of project root directory:'
                sh 'ls -al'
            }
        }

        stage('Build APK') {
            steps {
                script {
                    // Run Flutter commands for building APK
                    sh "/snap/bin/flutter clean"
                    sh "/snap/bin/flutter pub get"
                    sh "/snap/bin/flutter build apk"
                }
            }
        }

        stage('Deploy') {
            steps {
                script {
                    // Assuming fastlane is installed in the Jenkins environment
                    sh 'fastlane deploy_to_play_store'
                }
            }
        }
    }

    post {
        success {
            echo 'Build and deployment successful!'
        }
        failure {
            echo 'Build or deployment failed!'
        }
    }
}
